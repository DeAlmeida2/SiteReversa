import styled from "styled-components";

export const Container = styled.div`
    grid-area: FT;
    
   display: flex;
   justify-content: space-between;
   align-items: center;
   padding-left: 450px;
    
   > div {
    align-items: right;
    border-left: 1000px;
   }
`;